# Core framework (pin exact versions during implementation).
# Additional dependencies are added here as features are built.
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# Database layer
SQLAlchemy>=2.0
alembic
psycopg[binary]

# Auth & security
PyJWT>=2.8
bcrypt>=4.1
email-validator>=2.0

# AI (optional at runtime — the app degrades gracefully without it)
anthropic>=0.39

# HTTP client — website scrape fallback for brand extraction (also used by the test client)
httpx>=0.27

# Headless browser — renders client sites for brand extraction (text after JS,
# computed colors/fonts, screenshot for Claude vision). Needs a one-time
# `playwright install chromium` (run_local.sh and the Dockerfile handle this);
# without the browser the app degrades gracefully to the httpx scrape.
playwright>=1.45

# Testing
pytest>=8.0
